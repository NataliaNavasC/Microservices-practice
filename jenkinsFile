pipeline {
  agent any
  stages { 
        stage('deploy tourists') {
            steps{
              dir('TouristService'){
                sh 'mvn package'
                sh 'cp target/tourists.war /deploy'
              }
            }
        }
    }
}
