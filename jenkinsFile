pipeline {
  agent any
  stages { 
        stage('deploy gateway') {
            steps{
              dir('GatewayService'){
                sh 'mvn package'
                sh 'cp target/gateway.war /deploy'
              }
            }
        }
    }
}
